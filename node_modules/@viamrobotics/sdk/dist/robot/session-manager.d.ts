import { type Transport } from '@connectrpc/connect';
export default class SessionManager {
    private deferredTransport;
    static heartbeatMonitoredMethods: Record<string, boolean>;
    readonly transport: Transport;
    private currentSessionID;
    private sessionsSupported;
    private heartbeatIntervalMs;
    private host;
    private starting;
    private get client();
    constructor(host: string, deferredTransport: () => Transport);
    get sessionID(): string;
    private getSessionMetadataInner;
    reset(): void;
    private readonly backgroundHeartbeat;
    private heartbeat;
    getSessionMetadata(): Promise<Headers>;
    private applyHeartbeatMonitoredMethods;
    private static hasHeartbeatOption;
}
